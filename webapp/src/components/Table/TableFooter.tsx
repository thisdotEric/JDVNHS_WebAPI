import React, { FC } from 'react';
import './Table.scss';

interface TableFooterProps {}

const TableFooter: FC<TableFooterProps> = ({}: TableFooterProps) => {
  return <div></div>;
};

export default TableFooter;
